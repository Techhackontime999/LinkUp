# DEPRECATED: Use requirements.txt instead (it now supports all platforms via PEP 508 markers).
# This file is kept for backwards compatibility.
# The unified requirements.txt includes platform-specific dependencies
# (e.g., python-magic-bin on Windows) using sys_platform markers.
#
# See: https://www.python.org/dev/peps/pep-0508/#environment-markers

-r requirements.txt
